export default function (context) {
  // context.userAgent = process.server ? context.req.headers['user-agent'] : navigator.userAgent

  // this.$message("middleware", "success")
  // console.log("middleware")

  // context.alert("middleware")
  // context.$route.push({
  //   path: '/index'
  // })
}
